import Fun from "@/classes/Fun";

export default class FunManager {
  execute(fun: Fun): any {
    if (fun.isServiceInternal) {
      // TODO chiamata interna
    } else {
      // TODO chiamata esterna
    }
  }
}
